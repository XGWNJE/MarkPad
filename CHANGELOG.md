# MarkPad Changelog

所有值得用户或维护者关注的变更都记录在这里。扩展安装版本号以 `manifest.json` 的 `version` 为准。

版本格式遵循 `major.minor.patch`：

- `patch`：修复、视觉微调、文档或不改变功能边界的小改动。
- `minor`：新增用户可见功能、设置项或工作流。
- `major`：不兼容变更、存储迁移或需要用户额外操作的升级。

## [Unreleased]

## [0.3.0] - 2026-09-21

- 重做书签图标系统：默认按用户上传、仅按书签名称命中的内置图标库、网站声明资源和无图标状态解析；内置库初始为空，不再显示首字母、emoji 或通用占位图标。
- 图标工坊收敛为内置候选与多格式文件上传：支持安全 SVG 动画、PNG/APNG、GIF、JPG 和 WebP，保持原始动画；透明上传图标与网站图标均可选择原样、自动融合、黑、白或自定义背景，且支持再次编辑。
- 网站图标仅在书签卡片进入可见区域后按需解析 `icon`、Apple Touch、Manifest 和同源 favicon，并按完整书签 URL 缓存；移除 Chrome `_favicon`、Google 回退、iconfont、Iconify、SVG API、外部 SVG 搜索和大规模预生成图标库。
- 扩展网站访问权限改为 `<all_urls>`，用于读取已保存书签的网站图标声明和图标资源；移除仅服务旧图标搜索的 `favicon`、`scripting` 与定向网站权限。
- 重做卡片视觉：移除描边、扫描边框、卡面背景和渐变文字背景；保留卡片倾斜、缩放与文字展开动画。文本自动选取对比色，并新增字体、字号、字重与字距设置。
- 新增卡片圆角、卡片容器边距和卡片间距设置；容器边距统一作用于页面四周，桌面与移动端一致。
- 新增基于原生 WebGL2 的可选背景光效，适配浅深主题；页面不可见时暂停、减少动效时静态渲染、WebGL2 不可用时安全回退为纯色背景。

## [0.2.0] - 2026-09-20

- 移除主题菜单里的壁纸功能：6 张“静默索引”内置图、自定义图片上传、显示方式、壁纸亮度和模糊，以及 `assets/wallpapers/` 图片和对应专题文档一并删除（扩展体积减少约 10 MB）。设置里只保留浅色和深色两个按钮，`theme-init.js` 在首次绘制前写好 `<html data-theme>`，避免深色模式闪一帧亮色。
- 深色模式统一为中性深灰（背景 `#141414`、卡片 `#1e1e1e`），去掉原来的暖调；浅色保持温白，两边都只保留轻度对比。配色令牌由 `prefers-color-scheme` 媒体查询改为 `:root[data-theme="dark"]`，手动选择优先。
- 卡片文字改为默认收起、悬停时动画展开：文字面板从卡片底部浮出并盖在图标上，卡片尺寸和网格排布不变；键盘聚焦同样展开，触屏和手写笔第一次点按先展开文字、再点一次才打开。设置里的「卡片文字 显示/隐藏」开关连同 `showCardText` 偏好一起移除。
- 卡片边缘效果换成 MagicBento 风格光效：描边光晕跟着光标游走、柔和聚光层跟随指针、hover 粒子星点、3D 倾斜与磁吸、点击涟漪；颜色跟随亮色/深色主题，触摸设备、窄屏和系统「减少动效」下自动退回静态边框反馈。
- 内置 gsap 3.15.0 到 `vendor/`（`npm run vendor:gsap` 可重新生成），颜色集中在 `variables.css` 的 `--card-glow-*` 令牌，并在 `tests/card-effects.test.mjs` 里固定加载方式和接线。
- 方形卡片里的图标收缩到中间一块，品牌 SVG 不再贴到卡片边缘。
- 设置菜单收敛为主题、书签卡片和顶部栏三块：卡片尺寸与卡片背景强度滑块，加上顶部栏背景强度滑块。
- 放大顶部面包屑导航文字和触控区域，并让卡片尺寸滑块的每个档位产生明确的宽度变化。
- 默认书签图标改为本地图标库匹配，品牌图标由 Simple Icons 和 Iconify Logos 生成，通用工具/信息图标由 Lucide 生成；未命中时使用首字母兜底，不再自动批量刷新 favicon。
- 自定义图标拆分为 SVG 搜索和位图上传；位图上传要求原始尺寸至少 256×256。
- 增加图标解析、SVG 安全清理、存储分层和位图校验测试，并用生成脚本维护本地图标数据。
- 优化卡片右键菜单，将编辑、图标和删除操作分组，并用更直观的图标操作命名。
- 本地品牌图标改为生成全彩 SVG，并让标题完整短语优先匹配具体产品图标。
- 增加“图标：匹配本地图标”入口，展示标题、URL、域名、路径片段和命中依据，便于用户手动修正不准的默认图标。
- 扩充本地图标生成数据：新增 Iconify Logos 品牌补充库和 Lucide 通用图标库，覆盖非品牌的工具、文档、数据库、服务、终端、日历等场景。
- 替换应用图标：改为温白底 + 2×2 卡片网格（左上角一格墨色实心，其余三格中灰实心）的 Lumen 图标，去掉原来的绿→蓝→紫霓虹渐变，并同步更新 `icons/icon.svg`、16/48/128 PNG 和图标导出页。
- 修复 Chrome 账号书签模式下扩展不显示书签：根文件夹（书签栏/其他书签）ID 不再固定为 1/2/3，改为启动时从书签树动态解析。
- 当前文件夹在外部被删除时自动回退到根目录，不再抛出 "Can't find bookmark for id." 错误。

## [0.1.0] - 2026-07-04

- 统一产品名为 MarkPad，保留内部 `Bookmark*` 领域命名。
- 建立卡片式书签和文件夹网格，支持键盘、鼠标、触控/手写笔长按操作。
- 整理顶部工具栏：面包屑导航、图标化搜索、设置菜单，以及菜单内的新建书签/文件夹入口。
- 增加快速搜索、卡片右键操作、拖拽移动/删除、favicon 缓存和自定义图标能力。
- 增加图标工坊，用于多来源 SVG 搜索、预览和直接应用。
- 增加背景偏好、头部透明度和壁纸遮罩透明度设置。
- 增加版本记录规则，README 只展示当前版本摘要并链接完整变更记录。
